package main

func main() {
	// // Assertion on non-interface => err
	// name := "Sydney"
	// str, ok := name.(string) // this is a string, not an interface
	// if ok {
	// 	fmt.Printf("%q\n", str)
	// } else {
	// 	fmt.Printf("value is not a string\n")
	// }
}
